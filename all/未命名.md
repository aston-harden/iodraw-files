```mermaid
graph TD
    S([fa:fa-play 开始]) --> B[fa:fa-bucket 创建空桶数组 B[0..k-1]]
    B --> D[fa:fa-loop 遍历 A[i]\n放入桶 B[floor(A[i])]]
    D --> E{全部放完？}
    E -- 否 --> D
    E -- 是 --> F[fa:fa-sort 各非空桶\n内部排序]
    F --> G[fa:fa-link 按桶序号\n依次收集]
    G --> T([fa:fa-stop 结束])
```