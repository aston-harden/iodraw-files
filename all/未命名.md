```mermaid
flowchart TD
    A([开始]) --> B[创建空桶数组 B[0..k]]
    B --> C[遍历输入数组 A[0..n-1]]
    C --> D{将元素 A[i] 放入对应桶 B[floor(A[i])]}
    C -->|i++| C
    C -->|i=n| E[对每个非空桶 B[j] 内部排序]
    E --> F[依次收集各桶元素到输出数组]
    F --> G([结束])
    
```