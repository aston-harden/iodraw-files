```mermaid
flowchart TD
    A([开始]) --> B[创建空桶数组 B[0..k-1]]
    B --> C[遍历输入数组 A[i]\n把 A[i] 放入桶 B[floor(A[i])]]
    C --> D{所有元素\n已分桶？}
    D -- 否 --> C
    D -- 是 --> E[对每个非空桶 B[j]\n内部排序]
    E --> F[按桶序号 0→k-1\n依次收集元素]
    F --> G([结束])

```