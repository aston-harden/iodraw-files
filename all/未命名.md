```mermaid
@startuml
title 桶排序（Bucket Sort）——UML 活动图

start
:'输入：待排序数组 A[0..n-1]
  假设：元素值 ∈ [0, k)';

:'创建空桶数组 B[0..k-1]';

fork
  :遍历 A，将 A[i] 放入桶 B[floor(A[i])];
fork again
  :对每个非空桶 B[j] 内部排序\n（如插入排序、快排等）;
end fork

:'按桶序号 0→k-1 依次收集元素\n得到有序输出数组';

stop
@enduml

```